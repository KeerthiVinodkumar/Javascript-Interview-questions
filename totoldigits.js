// Find out total digits of a given number

function totoldigits(number){
    const digits=number.toString()
    const digitlength=digits.length
    return digitlength
}
console.log(totoldigits(12345)); 